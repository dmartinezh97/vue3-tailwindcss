export enum ButtonStyleEnum {
    DEFAULT = 'default',
    OUTLINED = 'outlined',
    ROUNDED = 'rounded',
    TEXT = 'text',
}

export enum ButtonTypeEnum {
    TEXT = 'text',
    EMAIL = 'email',
    PASSWORD = 'password',
    FECHA = 'date'
}

export enum ButtonShadowEnum {
    NONE = 'none',
    DEFAULT = 'default',
}